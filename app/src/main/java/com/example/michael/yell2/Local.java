package com.example.michael.yell2;

import java.util.List;

/**
 * Created by Michael on 3/30/16.
 */
public class Local {
    /*Update[] updates = {
            new Update("mk9", Calendar.getInstance()).addContent("First update"),
            new Update("mk9", Calendar.getInstance()).addContent("Second update"),
            new Update("mk9", Calendar.getInstance()).addContent("Third update"),
            new Update("mk9", Calendar.getInstance()).addContent("Fourth update"),
            new Update("mk9", Calendar.getInstance()).addContent("Fifth update"),
            new Update("mk9", Calendar.getInstance()).addContent("Sixth update"),
            new Update("mk9", Calendar.getInstance()).addContent("Seventh update"),
            new Update("mk9", Calendar.getInstance()).addContent(R.drawable.picture),
    };*/
    static DMActivity.ChatArrayAdapter chatArrayAdapter;
    static List<Update> DMArray;
}
