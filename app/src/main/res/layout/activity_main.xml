<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:id="@+id/parent_view"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/activity_main_swipe_refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!--<RelativeLayout
            android:layout_width="wrap_content"
            android:id="@+id/post_layout"
            android:layout_height="250dp">

            <EditText
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:id="@+id/edit_text_post"
                android:hint="Say something to the world..."
                android:layout_alignParentTop="true"
                android:layout_alignParentStart="true"
                android:layout_margin="8dp"
                android:textSize="18sp" />

            <Button
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/post_button"
                android:text="POST"
                android:layout_marginLeft="8dp"
                android:layout_marginRight="8dp"
                android:layout_alignTop="@+id/edit_text_post"
                android:layout_alignParentEnd="true"
                android:layout_toEndOf="@+id/edit_text_post" />

        </RelativeLayout>-->

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler"
            android:scrollbars="vertical"
            android:layout_below="@+id/post_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorBackground"/>

    </android.support.v4.widget.SwipeRefreshLayout>

    <ImageView
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:elevation="4dp"
        android:src="@drawable/ic_create_white_24dp"
        android:layout_alignParentBottom="true"
        android:id="@+id/writepost_image"
        android:layout_alignParentEnd="true"
        android:layout_margin="8dp"
        android:background="@drawable/background_back"/>

</RelativeLayout>
